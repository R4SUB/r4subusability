% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usability_indicators.R
\name{usability_indicators}
\alias{usability_indicators}
\title{Assess All Usability Indicators}
\usage{
usability_indicators(metadata, assets = character(0), ctx, config = NULL)
}
\arguments{
\item{metadata}{A data frame with columns \code{dataset}, \code{variable}, \code{label},
\code{origin}, \code{derivation} (optional), and \code{codelist} (optional). Compatible
with the \code{adam_metadata} and \code{sdtm_metadata} datasets from \code{r4subdata}.}

\item{assets}{A character vector of asset names present in the submission
package. Used by \code{\link[=assess_reviewer_guide]{assess_reviewer_guide()}}. Defaults to \code{character(0)}.}

\item{ctx}{A run context created by \code{\link[r4subcore:r4sub_run_context]{r4subcore::r4sub_run_context()}}.}

\item{config}{A configuration list from \code{\link[=usability_config_default]{usability_config_default()}}. If
\code{NULL} the default configuration is used.}
}
\value{
A validated evidence tibble combining results from all four
usability assessments (see \code{\link[r4subcore:as_evidence]{r4subcore::as_evidence()}}).
}
\description{
Convenience wrapper that runs all four usability assessments and returns a
combined evidence table. This is the primary entry point for the
\code{r4subusability} package.
}
\examples{
\donttest{
ctx <- r4subcore::r4sub_run_context(study_id = "STUDY01")
meta <- data.frame(
  dataset    = c("ADSL", "ADSL", "ADAE"),
  variable   = c("USUBJID", "AGE", "AETERM"),
  label      = c("Unique Subject Identifier", "Age", "Adverse Event Term"),
  origin     = c("CRF", "Derived", "CRF"),
  derivation = c(NA, "Derived from BRTHDTC and RFSTDTC", NA),
  stringsAsFactors = FALSE
)
assets <- c("ADRG", "define.xml")
ev <- usability_indicators(meta, assets = assets, ctx = ctx)
nrow(ev)
table(ev$result)
}

}
