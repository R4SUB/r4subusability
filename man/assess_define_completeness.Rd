% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usability_indicators.R
\name{assess_define_completeness}
\alias{assess_define_completeness}
\title{Assess Define-XML Completeness}
\usage{
assess_define_completeness(metadata, ctx, config = NULL)
}
\arguments{
\item{metadata}{A data frame with columns \code{dataset}, \code{variable}, \code{label},
\code{origin} (character), \code{derivation} (character, may be \code{NA}), and
\code{codelist} (character, may be \code{NA}).}

\item{ctx}{A run context created by \code{\link[r4subcore:r4sub_run_context]{r4subcore::r4sub_run_context()}}.}

\item{config}{A configuration list from \code{\link[=usability_config_default]{usability_config_default()}}. If
\code{NULL} the default configuration is used.}
}
\value{
A validated evidence tibble (see \code{\link[r4subcore:as_evidence]{r4subcore::as_evidence()}}).
}
\description{
Evaluates whether all datasets and variables in the metadata have the
required Define-XML fields populated (label, origin, derivation for derived
variables, and codelist reference where applicable). Results are returned as
R4SUB evidence rows with \code{indicator_id = "U-002"}.
}
\examples{
ctx <- r4subcore::r4sub_run_context(study_id = "STUDY01")
meta <- data.frame(
  dataset    = c("ADSL", "ADSL"),
  variable   = c("USUBJID", "AGE"),
  label      = c("Unique Subject Identifier", "Age"),
  origin     = c("CRF", "Derived"),
  derivation = c(NA, "Derived from BRTHDTC"),
  codelist   = c(NA, NA),
  stringsAsFactors = FALSE
)
ev <- assess_define_completeness(meta, ctx)
nrow(ev)

}
